/**
 * useOrganization — shortcut hook to consume OrganizationContext
 * Use anywhere inside OrganizationProvider tree
 */
export { useOrganizationContext as useOrganization } from '@/contexts/OrganizationContext';
